# Axis Decorate Labels Canvas

Demonstrates custom per-tick label styling on a canvas bar chart using `chartCartesian.xDecorate()`. Specific ticks are rendered with colored badge backgrounds while others remain plain, showing that axis decoration is always SVG regardless of the canvas plot area.
