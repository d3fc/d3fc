import axis from './axis';
import { ohlc, bar, candlestick, boxPlot, errorBar } from 'd3fc-shape';

export default {
    axis: axis,
    bar: bar,
    boxPlot: boxPlot,
    candlestick: candlestick,
    errorBar: errorBar,
    ohlc: ohlc
};
